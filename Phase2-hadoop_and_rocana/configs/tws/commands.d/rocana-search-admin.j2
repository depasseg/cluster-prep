#!/bin/sh

#setting java home
export JAVA_HOME="{{ java_jdk_home }}"

command_type=java
command_java_opts="-Djava.net.preferIPv4Stack=true"

# Uncomment the following line if you are using Kerberos
#command_java_opts="${command_java_opts} -Djava.security.auth.login.config=$ROCANA_CONF_DIR/jaas-client.conf"

# ensure that the path is correct on your system
CLASSPATH=${CLASSPATH}:/etc/hive/conf:/etc/hadoop/conf:${ROCANA_CONF_DIR}/rocana-search

command_java_main=com.rocana.search.admin.RocanaSearchAdminTool
command_project=rocana-search-admin
